# MAG COMPLEX

This is Basic Block Catalog type `378` (`MAG COMPLEX`). It processes real and imaginary components of complex-valued signals.

<!-- veragrid-block-introduction:start -->
**MAG COMPLEX** is a mathematical signal-processing primitive. It forms an algebraic relation between inputs, parameters, and outputs and is commonly combined with dynamic and nonlinear blocks to construct larger control equations without introducing an independent physical state.

## Typical use

- Use it to express the exact algebraic operation required by a controller or measurement chain.
- Check signal dimensions, signs, and zero-division or domain restrictions where applicable.
<!-- veragrid-block-introduction:end -->

## Behaviour

- Library location: `Native / Complex / Operations`.
- Inputs: 2.
- Outputs: 1.
- Declared states: 0.
- Configurable parameters: 0.
- The imported definition is fully supported by the Dynamic Editor catalogue.

## Characteristic equations

$$
0 = (mag - \sqrt{re \cdot re + im \cdot im})
$$

## Interface table

| Category | Name | Meaning | Units |
| --- | --- | --- | --- |
| Input | `re` | Input signal consumed by the block | model-dependent |
| Input | `im` | Input signal consumed by the block | model-dependent |
| Output | `mag` | Output signal produced by the block | model-dependent |

## Editing notes

Use General options for numeric parameter values and the DAE tab for symbolic variables and equations. Changing an Output flag only controls whether a variable is exported; it does not remove the variable from the model.
