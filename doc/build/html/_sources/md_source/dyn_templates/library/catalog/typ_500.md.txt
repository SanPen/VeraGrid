# Enable 5 sig

This is Basic Block Catalog type `500` (`Enable 5 sig`). It implements logical or event-driven signal behaviour.

<!-- veragrid-block-introduction:start -->
**Enable 5 sig** belongs to the hybrid/event layer of a dynamic model. It selects, stores, delays, or switches signals according to conditions, so its result depends on discrete mode or accepted simulation history in addition to the instantaneous continuous variables.

## Typical use

- Use it for protection, enable/disable sequences, sampled control, reset logic, and mode transfer.
- Define initial mode and boundary behavior explicitly to avoid unintended event chattering.
<!-- veragrid-block-introduction:end -->

## Behaviour

- Library location: `Native / Logic and Events / Timers and Enables`.
- Inputs: 6.
- Outputs: 5.
- Declared states: 0.
- Configurable parameters: 5.
- The imported definition is fully supported by the Dynamic Editor catalogue.

## Characteristic equations

$$
0 = (yo1 - (proc_{select\_0} \cdot yi1 + (1 - proc_{select\_0}) \cdot yi1_{default}))
$$

$$
0 = (yo2 - (proc_{select\_1} \cdot yi2 + (1 - proc_{select\_1}) \cdot yi2_{default}))
$$

$$
0 = (yo3 - (proc_{select\_2} \cdot yi3 + (1 - proc_{select\_2}) \cdot yi3_{default}))
$$

$$
0 = (yo4 - (proc_{select\_3} \cdot yi4 + (1 - proc_{select\_3}) \cdot yi4_{default}))
$$

$$
0 = (yo5 - (proc_{select\_4} \cdot yi5 + (1 - proc_{select\_4}) \cdot yi5_{default}))
$$

## Interface table

| Category | Name | Meaning | Units |
| --- | --- | --- | --- |
| Input | `yi1` | Input signal consumed by the block | model-dependent |
| Input | `yi2` | Input signal consumed by the block | model-dependent |
| Input | `yi3` | Input signal consumed by the block | model-dependent |
| Input | `yi4` | Input signal consumed by the block | model-dependent |
| Input | `yi5` | Input signal consumed by the block | model-dependent |
| Input | `Enable` | Input signal consumed by the block | model-dependent |
| Output | `yo1` | Output signal produced by the block | model-dependent |
| Output | `yo2` | Output signal produced by the block | model-dependent |
| Output | `yo3` | Output signal produced by the block | model-dependent |
| Output | `yo4` | Output signal produced by the block | model-dependent |
| Output | `yo5` | Output signal produced by the block | model-dependent |
| Parameter | `yi1_default` | Configurable model parameter | model-dependent |
| Parameter | `yi2_default` | Configurable model parameter | model-dependent |
| Parameter | `yi3_default` | Configurable model parameter | model-dependent |
| Parameter | `yi4_default` | Configurable model parameter | model-dependent |
| Parameter | `yi5_default` | Configurable model parameter | model-dependent |

## Editing notes

Use General options for numeric parameter values and the DAE tab for symbolic variables and equations. Changing an Output flag only controls whether a variable is exported; it does not remove the variable from the model.
