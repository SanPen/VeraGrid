# abc->dq0 (power invariant -- align a->d)

This is Basic Block Catalog type `544` (`abc->dq0 (power invariant -- align a->d)`). It converts signals between the coordinate systems or units identified by the block name.

<!-- veragrid-block-introduction:start -->
**abc->dq0 (power invariant -- align a->d)** is a coordinate-transformation block. In three-phase analysis, Clarke and Park transforms separate stationary or rotating components so sinusoidal phase quantities can be controlled as nearly constant d-q signals. The selected scaling determines whether amplitude or instantaneous power is preserved.

## Typical use

- Use it to connect phase-domain electrical quantities with d-q or sequence-domain control laws.
- Keep angle orientation, axis alignment, phase order, and power/amplitude convention consistent.
<!-- veragrid-block-introduction:end -->

## Behaviour

- Library location: `Native / Transforms / Clarke, Park and dq0`.
- Inputs: 4.
- Outputs: 3.
- Declared states: 0.
- Configurable parameters: 0.
- The imported definition is fully supported by the Dynamic Editor catalogue.

## Characteristic equations

$$
0 = (d - abc->dq0 (power invariant -- align a->d)_{sqrt2\_3} \cdot (\cos\left(theta\right) \cdot a + \cos\left(theta - abc->dq0 (power invariant -- align a->d)_{twopi\_3}\right) \cdot b + \cos\left(theta + abc->dq0 (power invariant -- align a->d)_{twopi\_3}\right) \cdot c))
$$

$$
0 = (q - abc->dq0 (power invariant -- align a->d)_{sqrt2\_3} \cdot (-\sin\left(theta\right) \cdot a - \sin\left(theta - abc->dq0 (power invariant -- align a->d)_{twopi\_3}\right) \cdot b - \sin\left(theta + abc->dq0 (power invariant -- align a->d)_{twopi\_3}\right) \cdot c))
$$

$$
0 = (zero - abc->dq0 (power invariant -- align a->d)_{sqrt2\_3} \cdot (abc->dq0 (power invariant -- align a->d)_{one\_sqrt2} \cdot a + abc->dq0 (power invariant -- align a->d)_{one\_sqrt2} \cdot b + abc->dq0 (power invariant -- align a->d)_{one\_sqrt2} \cdot c))
$$

$$
abc->dq0 (power invariant -- align a->d)_{sqrt2\_3}(t_0) = \sqrt{\frac{2}{3}}
$$

$$
abc->dq0 (power invariant -- align a->d)_{one\_sqrt2}(t_0) = (\frac{1}{\sqrt{2}})
$$

$$
abc->dq0 (power invariant -- align a->d)_{twopi\_3}(t_0) = (\frac{6.28319}{3})
$$

## Interface table

| Category | Name | Meaning | Units |
| --- | --- | --- | --- |
| Input | `a` | Input signal consumed by the block | model-dependent |
| Input | `b` | Input signal consumed by the block | model-dependent |
| Input | `c` | Input signal consumed by the block | model-dependent |
| Input | `theta` | Input signal consumed by the block | model-dependent |
| Output | `d` | Output signal produced by the block | model-dependent |
| Output | `q` | Output signal produced by the block | model-dependent |
| Output | `zero` | Output signal produced by the block | model-dependent |

## Editing notes

Use General options for numeric parameter values and the DAE tab for symbolic variables and equations. Changing an Output flag only controls whether a variable is exported; it does not remove the variable from the model.
