# K(1+sTd)/((1+sTa)(1+sTb)s) (parameter input)

This is Basic Block Catalog type `203` (`K(1+sTd)/((1+sTa)(1+sTb)s) (p`). It processes measurement or control signals for use inside a dynamic control model.

<!-- veragrid-block-introduction:start -->
**K(1+sTd)/((1+sTa)(1+sTb)s) (parameter input)** implements a feedback-control relation. Such blocks turn tracking error into an actuator command and may contain proportional, integral, filtering, or dynamic compensation terms. Their gains set closed-loop speed and damping rather than changing the underlying network physics directly.

## Typical use

- Use it inside voltage, current, power, speed, excitation, or governor control loops.
- Coordinate gains, limits, and time constants with the actuator and plant bandwidth.
<!-- veragrid-block-introduction:end -->

## Behaviour

- Library location: `Native / Control and Measurement / Controllers`.
- Inputs: 1.
- Outputs: 1.
- Declared states: 3.
- Configurable parameters: 6.
- The imported definition is fully supported by the Dynamic Editor catalogue.

## Characteristic equations

$$
\frac{d xa}{dt} = K(1+sTd)/((1+sTa)(1+sTb)s) (p_{dxa}
$$

$$
\frac{d xb}{dt} = (proc_{selfix\_2} \cdot \frac{K(1+sTd)/((1+sTa)(1+sTb)s) (p_{xaout} - xb}{Tb} + (1 - proc_{selfix\_2}) \cdot 0)
$$

$$
\frac{d xc}{dt} = K(1+sTd)/((1+sTa)(1+sTb)s) (p_{xbout}
$$

$$
0 = (K(1+sTd)/((1+sTa)(1+sTb)s) (p_{dxa} - (proc_{selfix\_0} \cdot \frac{K \cdot yi - xa}{Ta} + (1 - proc_{selfix\_0}) \cdot 0))
$$

$$
0 = (K(1+sTd)/((1+sTa)(1+sTb)s) (p_{xaout} - (proc_{selfix\_1} \cdot (xa + Td \cdot K(1+sTd)/((1+sTa)(1+sTb)s) (p_{dxa}) + (1 - proc_{selfix\_1}) \cdot K \cdot yi))
$$

$$
0 = (K(1+sTd)/((1+sTa)(1+sTb)s) (p_{xbout} - (proc_{selfix\_3} \cdot xb + (1 - proc_{selfix\_3}) \cdot K(1+sTd)/((1+sTa)(1+sTb)s) (p_{xaout}))
$$

$$
0 = (yo - (y_{min} + (xc - y_{min}) \cdot \Theta\left(xc - y_{min}\right) - (xc - y_{max}) \cdot \Theta\left(xc - y_{max}\right)))
$$

## Interface table

| Category | Name | Meaning | Units |
| --- | --- | --- | --- |
| Input | `yi` | Input signal consumed by the block | model-dependent |
| Output | `yo` | Output signal produced by the block | model-dependent |
| State | `K(1+sTd)/((1+sTa)(1+sTb)s) (p__xa` | Internal dynamic state | model-dependent |
| State | `K(1+sTd)/((1+sTa)(1+sTb)s) (p__xb` | Internal dynamic state | model-dependent |
| State | `K(1+sTd)/((1+sTa)(1+sTb)s) (p__xc` | Internal dynamic state | model-dependent |
| Parameter | `K` | Configurable model parameter | model-dependent |
| Parameter | `Td` | Configurable model parameter | model-dependent |
| Parameter | `Ta` | Configurable model parameter | model-dependent |
| Parameter | `Tb` | Configurable model parameter | model-dependent |
| Parameter | `y_max` | Configurable model parameter | model-dependent |
| Parameter | `y_min` | Configurable model parameter | model-dependent |

## Editing notes

Use General options for numeric parameter values and the DAE tab for symbolic variables and equations. Changing an Output flag only controls whether a variable is exported; it does not remove the variable from the model.
