# Timer (reset/hold reset/t0) (reset) incfw

This is Basic Block Catalog type `532` (`Timer (reset/hold reset/t0) _reset_incfw`). It generates or evaluates a time-dependent waveform.

<!-- veragrid-block-introduction:start -->
**Timer (reset/hold reset/t0) (reset) incfw** belongs to the hybrid/event layer of a dynamic model. It selects, stores, delays, or switches signals according to conditions, so its result depends on discrete mode or accepted simulation history in addition to the instantaneous continuous variables.

## Typical use

- Use it for protection, enable/disable sequences, sampled control, reset logic, and mode transfer.
- Define initial mode and boundary behavior explicitly to avoid unintended event chattering.
<!-- veragrid-block-introduction:end -->

## Behaviour

- Library location: `Native / Waveforms and Time / Time Sources and Timers`.
- Inputs: 2.
- Outputs: 1.
- Declared states: 1.
- Configurable parameters: 2.
- The imported definition is fully supported by the Dynamic Editor catalogue.

## Characteristic equations

$$
\frac{d x}{dt} = (proc_{select\_2} \cdot 1 + (1 - proc_{select\_2}) \cdot 0)
$$

$$
0 = (Timer (reset/hold reset/t0) _{reset\_incfw\_dir} - (proc_{selfix\_1} \cdot rst + (1 - proc_{selfix\_1}) \cdot (-rst + 1)))
$$

$$
0 = (Timer (reset/hold reset/t0) _{reset\_incfw\_tx} - (proc_{select\_4} \cdot x + (1 - proc_{select\_4}) \cdot proc_{lastvalue\_3}))
$$

$$
0 = (yo - (x - Timer (reset/hold reset/t0) _{reset\_incfw\_tx} + Timer (reset/hold reset/t0) _{reset\_incfw\_tinc}))
$$

$$
Timer (reset/hold reset/t0) _{reset\_incfw\_dir}(t_0) = (proc_{selfix\_0} \cdot rst + (1 - proc_{selfix\_0}) \cdot (-rst + 1))
$$

$$
Timer (reset/hold reset/t0) _{reset\_incfw\_tldf}(t_0) = glob_{time}
$$

$$
Timer (reset/hold reset/t0) _{reset\_incfw\_tx}(t_0) = 0
$$

$$
x(t_0) = 0
$$

$$
Timer (reset/hold reset/t0) _{reset\_incfw\_tinc}(t_0) = t0
$$

$$
yo(t_0) = t0
$$

## Interface table

| Category | Name | Meaning | Units |
| --- | --- | --- | --- |
| Input | `rst` | Input signal consumed by the block | model-dependent |
| Input | `t0` | Input signal consumed by the block | model-dependent |
| Output | `yo` | Output signal produced by the block | model-dependent |
| State | `Timer (reset/hold reset/t0) _reset_incfw__x` | Internal dynamic state | model-dependent |
| Parameter | `flank` | Configurable model parameter | model-dependent |
| Parameter | `t_start_delay` | Configurable model parameter | model-dependent |

## Editing notes

Use General options for numeric parameter values and the DAE tab for symbolic variables and equations. Changing an Output flag only controls whether a variable is exported; it does not remove the variable from the model.
