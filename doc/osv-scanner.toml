[[IgnoredVulns]]
id = "PYSEC-2018-34"
reason = "False positive for this manifest: doc builds consume VeraGridEngine from the local source tree, which requires numpy>=2.2.0, while this advisory is fixed in numpy 1.8.1."

[[IgnoredVulns]]
id = "PYSEC-2018-33"
reason = "False positive for this manifest: doc builds consume VeraGridEngine from the local source tree, which requires numpy>=2.2.0, while this advisory is fixed in numpy 1.8.1."

[[IgnoredVulns]]
id = "PYSEC-2017-1"
reason = "False positive for this manifest: doc builds consume VeraGridEngine from the local source tree, which requires numpy>=2.2.0, while this advisory is fixed in numpy 1.13.3."

[[IgnoredVulns]]
id = "PYSEC-2019-108"
reason = "False positive for this manifest: doc builds consume VeraGridEngine from the local source tree, which requires numpy>=2.2.0, while this advisory is fixed in numpy 1.16.1."

[[IgnoredVulns]]
id = "PYSEC-2021-856"
reason = "False positive for this manifest: doc builds consume VeraGridEngine from the local source tree, which requires numpy>=2.2.0, while this advisory is fixed in numpy 1.19.1."

[[IgnoredVulns]]
id = "PYSEC-2021-857"
reason = "False positive for this manifest: doc builds consume VeraGridEngine from the local source tree, which requires numpy>=2.2.0, while this advisory is fixed in numpy 1.19.0."

[[IgnoredVulns]]
id = "GHSA-fpfv-jqm9-f5jm"
reason = "False positive for this manifest: doc builds consume VeraGridEngine from the local source tree, which requires numpy>=2.2.0, while this advisory affects numpy 1.9.x."
