# VeraGridEngine Module: src/VeraGridEngine/IO/external_sparse_solvers/install_external_sparse_solver_runtime.py

- Original source path: `src/VeraGridEngine/IO/external_sparse_solvers/install_external_sparse_solver_runtime.py`
- Knowledge kind: generated VeraGridEngine code summary

## Module Summary

No docstring provided.

## Module Surface

- Class count: 0
- Top-level function count: 5
- Representative imports: __future__, argparse, os, subprocess, sys, pathlib

## Function: get_default_runtime_root()

Return the default root directory for external sparse-solver Python runtimes.

## Function: build_plugin_runtime_directory(plugin_name, destination_root)

Build the runtime installation directory for one plugin.

## Function: install_plugin_runtime(plugin_name, package_name, destination_root)

Install one sparse-solver runtime package to an external directory.

## Function: build_argument_parser()

Build the command-line parser.

## Function: main()

Install one external sparse-solver runtime package.
