# VeraGridEngine Module: src/VeraGridEngine/Topology/VoltageLevels/single_bar.py

- Original source path: `src/VeraGridEngine/Topology/VoltageLevels/single_bar.py`
- Knowledge kind: generated VeraGridEngine code summary

## Module Summary

No docstring provided.

## Module Surface

- Class count: 0
- Top-level function count: 7
- Representative imports: __future__, typing, VeraGridEngine.Devices, VeraGridEngine.Devices.multi_circuit, VeraGridEngine.enumerations

## Function: connect_bar_segments(grid, bar_buses, name)

:param grid:

## Function: create_single_bar_with_disconnectors(name, grid, n_bays, v_nom, substation, country, bar_by_segments, offset_x, offset_y, vl0)

Create a single-bar voltage level without disconnectors

## Function: create_single_bar(name, grid, n_bays, v_nom, substation, country, bar_by_segments, offset_x, offset_y, vl0)

Create a single-bar voltage level with disconnectors

## Function: create_single_bar_with_bypass_with_disconnectors(name, grid, n_bays, v_nom, substation, country, bar_by_segments, offset_x, offset_y, vl0)

Create a single-bar with by-pass voltage level

## Function: create_single_bar_with_bypass(name, grid, n_bays, v_nom, substation, country, bar_by_segments, offset_x, offset_y, vl0)

Create a single-bar with by-pass voltage level

## Function: create_single_bar_with_splitter_with_disconnectors(name, grid, n_bays, v_nom, substation, country, bar_by_segments, offset_x, offset_y, vl0)

Create a single-bar with splitter breaker voltage level

## Function: create_single_bar_with_splitter(name, grid, n_bays, v_nom, substation, country, bar_by_segments, offset_x, offset_y, vl0)

Create a single-bar with splitter breaker voltage level
